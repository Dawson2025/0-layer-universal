# VPS Setup Session - 2026-01-17

**Date:** 2026-01-17
**Status:** ✅ COMPLETE

## Session Overview

This session focused on enhancing VPS capabilities for remote Ubuntu management and setting up additional tools.

## What Was Set Up

### 1. Playwright MCP Server
- **Package:** `@playwright/mcp` (v0.0.56)
- **Purpose:** Browser automation for accessing web UIs (like Syncthing GUI)
- **Status:** ✅ Installed

```bash
# Installation
npm install -g @playwright/mcp@latest
npx playwright install chromium --with-deps
```

**Configuration:** `/root/.claude/mcp_servers.json`
```json
{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp", "--headless"]
    }
  }
}
```

### 2. Tailscale VPN
- **Purpose:** Connect VPS to same network as Ubuntu for direct SSH access
- **Status:** ✅ Installed and authenticated
- **VPS Tailscale IP:** `100.93.148.5`
- **Hostname:** `ubuntu-4gb-nbg1-1`
- **Account:** `2025computer2025@`

```bash
# Installation
curl -fsSL https://tailscale.com/install.sh | sh
tailscale up
```

### 3. Claude Code Permissions
- **File:** `/root/.claude/settings.local.json`
- **Status:** ✅ Configured for full access

```json
{
  "permissions": {
    "allow": [
      "Bash(*)",
      "Read(*)",
      "Write(*)",
      "Edit(*)",
      "Glob(*)",
      "Grep(*)",
      "WebFetch(*)",
      "WebSearch(*)",
      "Task(*)"
    ],
    "deny": []
  }
}
```

## Issues RESOLVED

### 1. Ubuntu Login Loop - FIXED ✅
- **Problem:** Ubuntu GUI login loops back to login screen
- **Cause:** GDM3 + NVIDIA hybrid graphics issue ("Session never registered, failing")
- **Solution:** Switched from GDM3 to LightDM
  ```bash
  apt install lightdm
  echo '/usr/sbin/lightdm' > /etc/X11/default-display-manager
  systemctl disable gdm3
  reboot
  ```
- **Status:** RESOLVED - Ubuntu now boots and logs in normally

### 2. TTY Access Not Working
- **Problem:** Ctrl+Alt+F1/F2/F3 not switching to text console
- **Possible causes:**
  - GNOME capturing the keys
  - Display manager configuration
  - Need to try F4, F5, or F6 instead

### 3. Claude Root Restriction
- **Problem:** `claude --dangerously-skip-permissions` can't run as root
- **This is a security feature, not a bug**
- **Solution:** Create non-root user or run without that flag

### 4. Ubuntu Not on Tailscale
- **Problem:** Ubuntu not appearing in Tailscale network
- **Current devices on Tailnet:**
  - VPS: `100.93.148.5` (ubuntu-4gb-nbg1-1)
  - iPhone: `100.75.210.27` (iphone171)
- **Solution needed:** Run `sudo tailscale up` on Ubuntu via TTY

## Next Steps

1. **Get TTY access on Ubuntu**
   - Try Ctrl+Alt+F4, F5, F6
   - Or boot into recovery mode from GRUB
   - Or use a live USB to access the filesystem

2. **Connect Ubuntu to Tailscale**
   ```bash
   sudo tailscale up
   ```

3. **Fix login loop** (common solutions)
   ```bash
   # Check disk space
   df -h

   # Fix .Xauthority
   rm ~/.Xauthority

   # Reinstall display manager
   sudo apt install --reinstall gdm3

   # Check for broken packages
   sudo apt --fix-broken install
   ```

4. **SSH into Ubuntu from VPS**
   ```bash
   ssh dawson@<ubuntu-tailscale-ip>
   ```

## Architecture After Setup

```
┌─────────────────┐     Tailscale      ┌─────────────────┐
│  iPhone         │◄─────────────────►│  Hetzner VPS    │
│  (Terminus)     │    100.75.210.27   │  100.93.148.5   │
└─────────────────┘                    │  46.224.184.10  │
                                       └────────┬────────┘
                                                │
                                       Tailscale (pending)
                                       Syncthing (pending)
                                                │
                                       ┌────────▼────────┐
                                       │  Ubuntu Native  │
                                       │  (Login Loop)   │
                                       │  100.200.164.45?│
                                       └─────────────────┘
```

## Files Modified

| File | Change |
|------|--------|
| `/root/.claude/mcp_servers.json` | Created - Playwright MCP config |
| `/root/.claude/settings.json` | Created - Global permissions |
| `/root/.claude/settings.local.json` | Updated - Full tool permissions |

## Related Documentation

- [STATUS.md](./STATUS.md) - Overall multi-OS sync status
- [VPS_CREDENTIALS.md](./VPS_CREDENTIALS.md) - VPS access details
- Linux setup: `/root/sync/dawson-workspace/code/0_layer_universal/layer_0/layer_0_02_sub_layers/sub_layer_0_05+_setup_dependant/docs/ubuntu-linux-setup.md`
