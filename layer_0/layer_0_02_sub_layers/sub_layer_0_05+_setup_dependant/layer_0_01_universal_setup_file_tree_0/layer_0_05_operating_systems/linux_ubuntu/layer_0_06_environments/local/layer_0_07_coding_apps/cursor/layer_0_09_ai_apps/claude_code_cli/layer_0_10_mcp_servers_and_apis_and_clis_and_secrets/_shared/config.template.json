{
  "tavily": {
    "api_key": "${TAVILY_API_KEY}",
    "description": "Tavily AI Search API key - obtain from https://tavily.com"
  },
  "context7": {
    "api_key": "${CONTEXT7_API_KEY}",
    "api_url": "${CONTEXT7_API_URL}",
    "description": "Context7 MCP server credentials - obtain from https://context7.com"
  },
  "_meta": {
    "template_version": "1.0.0",
    "instructions": [
      "1. Copy this file to config.local.json",
      "2. Replace ${VAR_NAME} placeholders with actual values",
      "3. Or set environment variables and use this file directly if your tool supports variable substitution",
      "4. Never commit config.local.json to version control"
    ]
  }
}
