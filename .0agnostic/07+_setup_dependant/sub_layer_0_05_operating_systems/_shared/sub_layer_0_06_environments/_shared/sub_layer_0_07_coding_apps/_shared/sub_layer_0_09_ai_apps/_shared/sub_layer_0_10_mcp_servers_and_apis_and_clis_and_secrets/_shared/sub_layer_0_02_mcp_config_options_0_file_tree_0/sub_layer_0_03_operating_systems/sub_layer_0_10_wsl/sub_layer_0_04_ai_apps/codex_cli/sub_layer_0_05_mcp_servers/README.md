---
resource_id: "b4555b7f-c21b-4abf-b4fb-d3f5384f10ad"
resource_type: "readme_document"
resource_name: "README"
---
# MCP Servers (Codex CLI on WSL)

This folder mirrors the top-level `0.05_mcp_servers/` list, but is scoped to **Codex CLI on WSL/WSLg**.

Each server subfolder contains:
- WSL/Codex-specific notes (what breaks, what flags/env are required)
- Pointers back to the canonical server docs under `../../../../0.05_mcp_servers/`

Use this when configuring Codex MCP via `~/.codex/config.toml` and `automation/scripts/codex_mcp_sync.py`.
