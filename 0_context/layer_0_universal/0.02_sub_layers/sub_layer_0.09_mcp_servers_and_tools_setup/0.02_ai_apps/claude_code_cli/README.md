# Claude Code CLI MCP Setup

## Config location
- `~/.claude.json` (stores MCP server configs per project, plus other Claude settings)
- CLI commands exist for managing MCP servers (e.g., `claude mcp add ...`)

## Recommended pattern
- Prefer the universal wrapper scripts generated by `automation/scripts/mcp_manager.py --scope user`
- For Playwright on WSLg, use a `--config` JSON approach (see `mcp_servers/playwright-mcp/INSTALL.md`)

## Notes
- If you add MCP servers “per project” via Claude’s CLI, document the choice in the relevant project layer handoff docs.

## References
- `core-system/README.md` (integration notes)
- `mcp_servers/playwright-mcp/INSTALL.md`
- `core-system/CONTEXT7_CLAUDE_SETUP.md` (if present in this repo’s MCP docs)
