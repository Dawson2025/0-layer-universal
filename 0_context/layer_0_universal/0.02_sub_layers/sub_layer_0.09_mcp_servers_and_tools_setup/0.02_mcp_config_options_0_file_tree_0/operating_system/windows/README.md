# Windows MCP Notes

## Scope
Use this when running AI tools on Windows directly (not WSL).

## Key prerequisites
- Node.js installed and available in PATH.
- Playwright browsers installed (Playwright-managed cache).

## Runbooks
See `ai_apps/` for per-app MCP setup instructions on Windows.
