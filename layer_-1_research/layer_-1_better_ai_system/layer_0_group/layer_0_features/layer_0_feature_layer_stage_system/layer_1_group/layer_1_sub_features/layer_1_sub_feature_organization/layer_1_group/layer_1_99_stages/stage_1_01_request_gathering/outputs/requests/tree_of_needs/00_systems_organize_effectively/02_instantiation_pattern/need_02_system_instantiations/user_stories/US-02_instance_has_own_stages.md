# Instance Has Own Stages

**As a** system,
**I want** each instance to have its own stage progression,
**So that** per-user work (assessments, learning paths) follows structured stages.

## Acceptance Criteria

- [ ] Instance has layer_N_99_stages/ with stage lifecycle
- [ ] Stages can track instance-specific progress
- [ ] Stage agents can access both instance and system context
