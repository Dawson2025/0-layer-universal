# Same Pattern Recursively

**Need**: [Nested Systems](../README.md)

---

- MUST apply the R/P/I pattern at every nesting level
- MUST ensure each sub-system can have its own research, production, and instances
- SHOULD use the same entity structure at every level (consistent conventions)
- SHOULD NOT require different organizational patterns at different nesting depths
