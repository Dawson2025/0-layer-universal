# Pointer Sync System — Design

> **Canonical location**: Root `.0agnostic/03_protocols/pointer_sync_protocol.md`

The design of the pointer sync system lives at the root `.0agnostic/` level because it is a universal system. This pointer connects the stage_3_04_design stage to the canonical protocol document.

## Related Design Artifacts

| Artifact | Canonical Location |
|----------|-------------------|
| Protocol (format, algorithm, workflow) | `.0agnostic/03_protocols/pointer_sync_protocol.md` |
| Knowledge (architecture overview) | `.0agnostic/01_knowledge/pointer_sync/pointer_sync_knowledge.md` |
